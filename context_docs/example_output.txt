$ explain_regex '/[^A-Za-z0-9_.]/'
any single character except a character from "A" to "Z", a character from "a" to "z", a character from "0" to "9", "_", "."

$ explain_regex '/^\d+$/'
the start of the string then a digit (0–9) one or more times then the end of the string

$ explain_regex '/hello|world/i'
"hello" or "world". Flags: case-insensitive
